name: go_router_examples
description: go_router examples
version: 3.0.1
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  adaptive_navigation: ^0.0.10
  collection: ^1.19.1
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  go_router:
    path: ..
  logging: ^1.3.0
  shared_preferences:
    git: https://github.com/kumsumit/shared_preferences.git
  url_launcher:
    git: https://github.com/kumsumit/url_launcher.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
